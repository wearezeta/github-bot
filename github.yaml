server:
  applicationConnectors:
    - type: http
      port: 8080

  requestLog:
    appenders: []

auth: "your_token"

postgres:
  host: localhost
  port: 5432
  database: postgres
  driver: postgresql
  user: postgres
  password: password

db:
    host: localhost
    port: 6379 #redis