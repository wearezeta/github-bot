sudo: required
language: java
# Cache our Gcloud SDK between commands
cache:
  directories:
  - "$HOME/google-cloud-sdk/"
services:
- docker
after_success:
- if [[ "$TRAVIS_BRANCH" == "master" ]]; then
  docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD ;
  docker build -t dejankovacevic/github-bot:latest . ;
  docker push dejankovacevic/github-bot ;
fi

before_install:
- openssl aes-256-cbc -K $encrypted_941c9d24308c_key -iv $encrypted_941c9d24308c_iv
  -in client-secret.json.enc -out client-secret.json -d

# If the SDK is not already cached, download it and unpack it
- if [ ! -d ${HOME}/google-cloud-sdk ]; then
     curl https://sdk.cloud.google.com | bash;
  fi

#- if [ ! -d ${HOME}/google-cloud-sdk/bin/kubectl ]; then
#     gcloud components install kubectl;
#  fi
  
- mkdir -p lib

- gcloud auth activate-service-account --key-file client-secret.json

script:
#- kubectl delete pod -l name=github -n staging
